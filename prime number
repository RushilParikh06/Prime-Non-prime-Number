#include<stdio.h>
#include<math.h>

int prime(int n){
    int flag =1;
    for(int i=2;i<sqrt(n);i++) if(n%i==0) flag=0;}

void main() {
    prime(49)?printf("prime"):printf("non prime"); //enter number here
}
